# FlowX 产品架构 v2 —— 六步流水线 · 平台级生产线 · 可扩展矩阵

> 本文是 FlowX 产品化重构的**总纲（北极星）**。Claude Code 照此分步施工，技术顾问（Chat）照此质检与派活。
> 与既有文档冲突时，**以本文为准**。
> 编写日期：2026-07-09 ｜ 版本：v2.0

---

## 0. 这份文档的地位

- **给谁看**：Code 按本文施工；Chat 按本文出任务、审产出、把关方向；Hans 按本文验收。
- **与既有文档的关系**：
  | 文档 | 状态 | 说明 |
  |---|---|---|
  | `ENGINE_API.md` | ✅ 仍有效 | 引擎可复用函数清单，**复用不重写**的依据 |
  | `产品化规划.md` | 📌 被本文扩展 | v1 的 4 页面思路，本文升级为 6 步 + 矩阵 |
  | `QUALITY_CHECK.md` | 📌 并入第 3 步 | 质检细则仍用，**新增"去 AI 味/过检测"一维** |
  | `MULTI_PLATFORM.md` | 📌 并入本文 | 多平台架构并进"平台级生产线 + publish_task" |
  | 本文 `产品架构_v2.md` | ⭐ 总纲 | 冲突以此为准 |

---

## 1. 核心原则

1. **半自动、人机协作**：机器干重活，人在「选题勾选」和「发布前把关」两个节点控质量。早期尤其以人工把控质量为主。
2. **可扩展第一（灵活、不卡死）**：平台 / 账号 / 赛道全部做成 **配置 + 插件**。
   - 加一个新平台 = 加一个 publisher/adapter 文件 + `REGISTRY` 注册一行 + 一条 accounts 配置，**不改主流程、不动其它平台**。
   - **禁止在业务代码里硬编码平台名清单**（如 `if platform == "toutiao"`）。现有 `publishers/base.py`（抽象基类）+ `publishers/__init__.py`（`REGISTRY` 工厂）就是范例，本文把这套"抽象 + 注册表"模式**推广到排版、上传、选题各环节**。
   - 六步的划分本身也不写死——未来新增步骤/平台时按需优化。
3. **复用不重写**：`hotspot / research / generate / publishers / store` 引擎已验证可用，界面层/接口层只负责**编排**这些函数。
4. **小步走**：一模块一验证一 `git commit`。
5. **合规红线**：禁止商用、仅个人自用；去 AI 味 / 过 AI 检测；不做绝对化承诺、不编造数字；注意配图/素材版权。

---

## 2. 六步流水线

```
① 选题扫描 → ② 内容生产 → ③ 质检 → ④ 适配排版 → ⑤ 上传 → ⑥ 自动化发布
```

**关键区分**：④ 排版 ≠ ⑤ 上传 ≠ ⑥ 发布。
- **上传 = 把内容推到各账号后台「存草稿」**（进草稿箱，尚未公开）。
- **发布 = 真正发出去**（早期人工点、成熟后定时/批量）。
- 这套"草稿 → 发布"两段式正好贴合公众号/头条后台的机制。

| 步骤 | 输入 | 处理 | 产物 | 现有模块 | 待建 |
|---|---|---|---|---|---|
| ① 选题扫描 | 平台赛道开关 | 抓百度/头条热榜 → 按赛道分类，命中即全列不限量 | 分组热点列表 | `hotspot/` ✅ | — |
| ② 内容生产 | 勾选的热点 | 搜索素材（+可选采集同题爆文洗稿）→ DeepSeek 改写 → 配图 | 稿件（标题+正文+配图） | `research/` `generate/` ✅ | 采集/洗稿（后置）、视频（后置，留接口） |
| ③ 质检 | 稿件 | 规则检测 + AI 检测 **+ 去 AI 味/过检测** → 绿/黄/红分档，红档拦截 | 质检分+问题+档位 | 复用 `generate.llm` | `quality/check.py`（新建） |
| ④ 适配排版 | 质检通过稿 | 按目标平台改格式/调性（头条纯文本 / 公众号 HTML / 小红书短文+标签），产出每平台一版 | 平台适配版内容 | — | `render/adapt`（新建，可插拔） |
| ⑤ 上传 | 排好版的内容 | 推到该平台下各账号后台**存草稿** | 各账号草稿 + 草稿态记录 | `publishers/`（扩展存草稿能力） | per 账号草稿逻辑 |
| ⑥ 自动化发布 | 草稿 | 触发 publish（人工/定时/批量）→ 记状态 | 已发 + 链接 | `publishers/` ✅（头条已实测） | 定时/批量调度 |

> ②里的"素材采集/洗稿"、视频线，都是**先留接口后填**，不阻塞 MVP。

---

## 3. 横向 × 纵向 矩阵 · 平台级生产线（Pipeline）

- **横向**：今日头条 / 百家号 / X / 小红书 …（先文章类，视频类后置）。
- **纵向**：同一平台下多账号（头条账号1 / 账号2 / 账号3 …，可陆续加）。

### 独立粒度 = 平台级（已拍板）
一条 **Pipeline（内容生产线）= 一个平台**，定义为：
```
Pipeline = {
  platform,                 # 绑定的平台
  选题/赛道配置,             # 该平台跑哪些赛道
  prompt 模板,               # 该平台的调性/风格
  稿件存储命名空间,          # 该平台稿件独立分区（不与别的平台混）
  队列/状态                  # 独立的生产队列与状态
}
```
- **一个选题 → 每个平台生产一版适配内容**（平台之间互不写入、不交叉）。
- **账号只在 ⑤上传 / ⑥发布 两步才展开**：该平台的一版内容 → 发到这个平台下的多个账号（账号1/2/3 共用同一版内容，区别只是发布单位）。

### 共享 DeepSeek、独立不交叉
- **共享**：DeepSeek 只有一个 client（一个 key、一处配置），在基础设施层，所有 pipeline 共用这台"发动机"。
- **独立**：每条 pipeline 的选题池、prompt、稿件分区、状态彼此隔离。发动机共用，"物料和产线"隔开。

### 平台分两类（统一接口，插件实现）
| 类别 | 平台 | 实现方式 | 接口 |
|---|---|---|---|
| 浏览器自动化类 | 头条 / 百家 / 小红书 | Playwright + 独立 profile/cookie（每账号一份登录态） | `publish(article) -> PublishResult` |
| 官方 API 类 | X / 微信公众号 | 调官方接口（token/素材上传等）；X 有官方写接口但分档收费，需确认当前档位 | 同上（`override publish`） |

> 两类都实现同一个 `publish(article) -> PublishResult`；`base.py` 需小重构，把"浏览器启动"从 publish 主流程解耦，让 API 类不必走浏览器。（见 `MULTI_PLATFORM.md` 第二节）

---

## 4. 数据模型（MVP → 扩展的迁移路径）

**原则**：MVP 阶段（单平台单账号）先用现有单表，只加必要字段；多平台/多账号所需的表在对应阶段再引入，避免过早复杂化。

### accounts（一平台多条）
```
name          # 账号唯一名，如 hans_toutiao_1
platform      # toutiao / baijiahao / xhs / x / wechat ...
account_no    # 该平台下第几个号（1/2/3…）
profile_dir   # 浏览器类：登录态目录
credentials   # API 类：token/appid/secret 等（加密存本地）
enabled
```

### articles（在现有表上加字段）
现有：`id, title, body, image, track, source, status, created_at`
- **MVP 就加**：
  - `platform`（MVP 恒为 `toutiao`，为横向扩展预留）
  - `qc_score INTEGER` / `qc_level TEXT(green/yellow/red)` / `qc_problems TEXT(JSON)`（质检，见 `QUALITY_CHECK.md`）
  - `status` 增加取值 `待修复`（红档）
- **④排版阶段加**：排版产物（`rendered_html` 或独立 `rendered` 表，按平台存适配版）
- `status` 建议演进为：`草稿(生产完) / 待修复(红档) / 已排版 / 已上传 / 已发 / 失败`

### publish_task（平台扩展阶段才引入）
一篇稿 × 一个目标账号 = 一条发布任务：
```
article_id, account_name, platform, status(未发/已发/失败),
draft_saved(是否已存草稿), url, error, published_at
```
> 头条账号1 成功、小红书账号2 失败，互不影响，各记各的状态。

### "什么时候加什么"速查
| 里程碑 | 数据模型动作 |
|---|---|
| MVP（单平台单账号） | articles 加 `platform` + `qc_*` 字段；沿用单表 |
| 加第 2 个平台 | 引入 `publish_task`；render/adapt 平台适配；accounts 多平台 |
| 多账号 | accounts 多条（同平台多 account_no）；上传/发布按账号展开 |
| 视频线 | 新增视频产物存储 + 视频 publisher |

---

## 5. 分阶段路线（落地顺序）

### 阶段 1 — MVP：单平台（头条）单账号，六步闭环
- **1a　接生成链**：页2 勾选 → `POST /generate` → 页3 渲染真实稿件卡片。**← Code 当前第一任务**
- **1b　质检模块**：新建 `quality/check.py`（规则 + AI + **去 AI 味/过检测**），插进 `/generate`，页3 展示真徽章（绿/黄/红），红档进"待修复"拦截。
- **1c　排版 + 上传 + 发布**：④头条适配排版（纯文本规整）→ ⑤存草稿 → ⑥发布页接 `/articles`、`/publish` 真发头条。

### 阶段 2 — 横向加平台
百家号 → 小红书 → X。每加一个 = 填一个 publisher + 一张平台卡片 + render 适配。
引入 `platform` 分区 + `publish_task`。
> ⚠️ 小红书运营坑：本地签名在**多账号**下会出问题，多账号分发需自建签名服务（见对标 social-auto-upload）。

### 阶段 3 — 纵向多账号
accounts 同平台多条；上传/发布按账号展开。配套运营策略：养号（新号别批量发）、错峰、发布频率限制、模拟人工、登录态维护。

### 阶段 4 — 视频线 + 打包
接 MoneyPrinterTurbo 思路（LLM 脚本 → TTS → 字幕 → 素材 → 合成）；pywebview 套壳 + py2app/PyInstaller 打包成桌面 App。

---

## 6. 加新平台 · 可扩展性检查清单

每加一个平台，照此逐项，**不改主流程、不动其它平台**：
- [ ] `publishers/<platform>.py` 实现 `BasePublisher`（API 类则 `override publish`）
- [ ] `publishers/__init__.py` 的 `REGISTRY` 注册一行
- [ ] 排版适配：`render/adapt` 加该平台的调性/格式适配（可插拔）
- [ ] `accounts.yaml` 加该平台账号（可多个）
- [ ] 若该平台有独立赛道/ prompt，加一条 pipeline 配置
- [ ] 浏览器类：`playwright codegen` 录制选择器；API 类：接官方接口
- [ ] 自测发布 → 反馈给 Chat 质检

---

## 7. 角色与协作

| 角色 | 职责 |
|---|---|
| **Hans**（产品/老板） | 定方向、拍板决策、验收 |
| **Chat**（技术顾问/PM） | 统筹项目规划、质检、写 Code 提示词、审 Code 产出、派新任务、把关方向 |
| **Code**（施工队） | 按提示词实现，能自主完成的尽量自主完成，完成后反馈 |

**协作循环**：
```
Chat 出任务(含提示词) → Code 实现 + 自测 → Code 反馈(改了什么/怎么测/有无偏差)
   → Chat 质检 → 通过则派下一任务 / 不通过则给修正提示词
```
> Code 每次反馈请带上：改动清单、如何测试、与要求的偏差（如有）。

---

## 8. GitHub 对标（备查，挑对我们有用的）

| 项目 | 用途 | 我们借鉴什么 |
|---|---|---|
| **AIWriteX** | 与本项目需求几乎 1:1（热搜聚合+爆款选题+洗稿+去AI味过朱雀+多平台+多账号+多智能体） | ①去 AI 味/过检测并入质检 ②多智能体流水线拆法 ③软件/开发双模式 ④它承认 AI 全自动长文过检测越来越难 → 印证"早期人工把控质量" |
| **social-auto-upload** | 多账号矩阵分发（`--account` per 账号、每账号独立 cookie、定时/矩阵） | 纵向多账号的范式；⚠️ 小红书多账号需自建签名服务 |
| **MoneyPrinterTurbo**（6万+ star） | 全自动短视频（LLM 脚本→TTS→字幕→素材→合成，支持 DeepSeek/Pexels） | 阶段 4 视频线的头号参考，栈与本项目相通可平滑接 |
| **awesome-ai-media-cn** | 150+ AI 自媒体/视频工具精选清单 | 后续挑组件（TTS/数字人/字幕/竞品爬虫）时来这翻 |
