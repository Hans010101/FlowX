# FlowX 项目交接文档

> 面向:接手继续开发/优化的工程师
> 版本时间:2026-07
> 原产品负责人:Hans(PM/验收) · 原技术顾问:Claude(规划/质检) · 原施工:Claude Code

---

## 一、这是什么

**FlowX = 面向今日头条(及多平台)的自媒体内容自动化工具。**

一句话描述链路:

> 抓热点 → AI 写稿 → AI 配图 → AI 质检 → 定向优化 → 稿库 → 同步到平台草稿箱 → 人工确认发布

**已实现"定时无人值守"**:每天定点自动跑完 选题→生成→质检→绿档进草稿箱,人只需最后去平台点发布。

**运行形态**:本地桌面工具(macOS)。Python FastAPI 后端 + 原生 HTML/CSS/JS 前端,浏览器访问 `http://127.0.0.1:8000`。

---

## 二、快速上手(接手第一步)

```bash
cd <项目目录>
.venv/bin/python -m uvicorn app:app --reload
# 浏览器打开 http://127.0.0.1:8000
```

**依赖的外部服务/条件**:

| 依赖 | 用途 | 缺了会怎样 |
|---|---|---|
| DeepSeek API Key | 写稿(必需) | 出不了文 |
| Tavily API Key | 搜索素材(默认) | 走博查兜底 |
| 博查 API Key | 搜索素材(付费兜底) | Tavily 不达标时无兜底 |
| Pexels API Key | 配图兜底 | 候选图用尽时无兜底图 |
| DailyHotApi(Docker 容器) | 抖音/微博/知乎/B站/36氪/澎湃 热榜 | 只剩百度/头条(直抓,不受影响) |
| **Wechatsync 浏览器扩展 + 登录态** | **发布(关键)** | **发不出去,见"已知痛点"** |
| @wechatsync/cli (npm 全局) | 后端自动发布 | ⚡自动发布不可用 |

所有 Key 在 `.env`。**`.env` 只在服务启动时读取,改完必须重启 uvicorn**。

---

## 三、技术栈与目录

Python 3.12 + FastAPI + APScheduler + SQLite,前端原生 HTML/CSS/JS(单文件 `static/index.html`)。

```
app.py                  # FastAPI 全部路由
config.py               # .env / settings.yaml / tracks.yaml / accounts.yaml 读写
settings.yaml           # 热点源、搜索阈值、自动流水线配置
tracks.yaml             # 9个赛道:关键词 + 写作 prompt + 开关 + 顺序
accounts.yaml           # 账号配置(Playwright 老路遗留,当前发布不用)
.env                    # 各种 API Key(不入库)

hotspot/
  fetch.py              # 多源抓取(_FETCHERS 可插拔 + fetch_all)、跨平台合并、热度透传
  classify.py           # 按 tracks.yaml 关键词归类
research/
  search.py             # Tavily 优先 → 博查兜底(search_with_fallback)
generate/
  llm.py                # DeepSeek 客户端(chat)
  writer.py             # 写稿、清洗(_LEAK_PATTERNS/_clean_body/_extract_title)
  illustrate.py         # 配图:og优先+过滤+候选池+语义弱排序
  revise.py             # 定向优化(防编造红线)
quality/
  check.py              # rule_check + ai_check + quality_check(绿黄红分档)
store/
  db.py                 # SQLite:articles 表、processed_topics 表
publishers/             # ⚠️ Playwright 老路,已冻结不用(见"历史包袱")
static/index.html       # 全部前端(单文件)
logs/auto_runs.jsonl    # 自动流水线运行记录
output/images/          # 配图落地
product_mockup_v4.html  # UI 设计基准稿(v4,现界面照它做的)
```

---

## 四、核心链路详解

### ① 选题
- **多源**:百度/今日头条(官方直抓) + 抖音/微博/知乎/B站/36氪/澎湃(经本机 DailyHotApi 容器,127.0.0.1:6688)。
- **跨平台合并**:同话题合并,标注多来源(「百度 · 头条」),SIM_THRESHOLD=0.82 模糊匹配。
- **热度透传**:头条有 `HotValue`、聚合源有 `hot`,合并时组内取 max;**百度榜无热度字段**(实查确认),优雅留空不造假。**未做全局排序**——各平台热度量纲不可比(头条4700万 vs 澎湃269),硬排会失真。
- **赛道归类**:按 tracks.yaml 关键词。9个赛道:民生/体育/娱乐/科技/财企/时事/健康/历史/美食。
- **6小时去重**:写过稿的热点记入 `processed_topics`(带时间戳),6小时内不再采;超6小时可重新采。手动抓取和自动流水线双入口都应用。
- 前端每赛道最多展示 10 条;赛道芯片可拖拽排序(持久化在 tracks.yaml 顶层 `track_order`),顺序驱动结果分组顺序。

### ② 生成
- 搜索素材(Tavily→博查兜底,阈值 min_results=3/min_chars=300 可在设置页调)。
- DeepSeek 写稿,按赛道 prompt 定调性。
- **配图**:og:image/twitter:image 优先 → 正文区图 → 兜底;过滤(URL黑名单 logo/banner/ad 等、尺寸<300px、宽高比>3:1);收集**有序候选池**存 `img_candidates`,按标题↔alt/URL 关键词命中**弱排序**;候选用尽退 Pexels。
- ⚠️ 历史坑:旧逻辑挑"文件最大的图"导致抓到 banner,已改为优先级排序(commit 6d51757)。

### ③ 质检 `quality/check.py`
- 规则检查 + AI 检查(5个维度 + problems 数组)。
- 分档:总分≥80且无严重项=**green**;60-79=**yellow**;<60或命中严重规则=**red**(红档拦截进"待修复",不给发布按钮)。
- AI 解析失败降级 75(不挂红),卡片显示「⚠ AI检测未生效(仅规则)」。
- **重要口径**:ai_check 对"缺来源/未证实"类问题,只建议**删除/软化**,**禁止建议"补充来源"**——与 /revise 的防编造红线对齐(否则质检催补来源、优化又不许补,把人往编造带)。

### ④ 定向优化 `/revise`
- 按质检问题二次修订。**红线:缺来源只能删除或软化,绝对禁止新增来源/机构/日期/数字。**
- 改标题会导致 id 变更(id=md5(title)[:12]),前端 data-id 跟着更新、旧行删除。
- 失败保留原稿。

### ⑤ 换图 `/reimage`
- 按 `image_idx` 依次取候选池下一张,用尽退 Pexels。
- **不触发重新质检**(只换图,qc_*/status/created_at 不变)。

### ⑥ 稿库与发布
- 状态:未发 / 已发 / 待修复。**「✓ 标记已发」是手动的**——见"设计取舍"。
- **发布双通道**(都进平台草稿箱,不直接公开):
  - **🚀 同步发布(路1/手动)**:打开 `/article/{id}` 干净预览页 → 用户点浏览器里的 Wechatsync 扩展图标 → 提取同步。
  - **⚡ 自动发布(路2/自动)**:后端 `POST /sync` → subprocess 调 `wechatsync sync <html> -p <平台> --cover <url>` → 进草稿箱,回显草稿链接。

### ⑦ 自动流水线(定时无人值守)
- APScheduler CronTrigger,`settings.yaml` 的 `auto` 段配置(enabled/schedule/count/min_level/platforms/tracks/auto_publish)。
- 流程:选题→生成→质检→**按质量闸筛选(默认只 green)**→CLI 进草稿→记录。
- **`/auto/run` 已异步化**(Lock 互斥 + daemon 线程,接口 0.009s 返回),定时与手动共用同一把锁,避免并发跑两轮。
- **改配置免重启**:`reschedule_job` 实现改时间即时生效。
- 界面:选题页顶部状态条(定时开关/下次运行/上次摘要/▶立即运行一次,5秒轮询);设置页 auto 区块全可编辑。
- 记录:`logs/auto_runs.jsonl` + `GET /auto/runs` + `GET /auto/status`。

---

## 五、接口清单

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/hotspots` | 抓热点(按 enabled_tracks),含合并/热度/6小时去重 |
| POST | `/generate` | 生成(含搜索/配图/质检/入库),生成后记 processed_topics |
| GET | `/articles` | 稿库列表 |
| POST | `/revise` | 定向优化 |
| POST | `/reimage` | 换图 |
| POST | `/recheck` | 重新质检 |
| POST | `/articles/{id}/status` | 标记已发/改回未发(白名单校验) |
| GET | `/article/{id}` | 干净文章预览页(HTML,供扩展提取) |
| POST | `/sync` | 后端调 CLI 发草稿(多平台) |
| GET | `/settings` | 全部配置(**key 只返 bool,绝不返回 key 值**) |
| POST | `/settings/sources` | 热点源开关写回 |
| POST | `/settings/tracks` | 赛道开关写回 |
| POST | `/settings/research` | 搜索阈值写回 |
| POST | `/settings/track-order` | 赛道顺序写回 |
| POST | `/settings/auto` | 自动流水线配置写回(含免重启改时) |
| POST | `/auto/run` | 手动触发一轮(异步) |
| GET | `/auto/status` | 自动流水线状态(轻量,前端轮询用) |
| GET | `/auto/runs` | 运行记录 |
| POST | `/publish` | ⚠️ **Playwright 老路,已冻结,内含 input() 阻塞闸,从 HTTP 调会卡死服务进程,不要用** |

---

## 六、必须遵守的红线(前人踩过坑的)

1. **key 绝不返回前端、绝不进日志**。`/settings` 只返回 `是否已配置` 的 bool。CLI 的 token 只进子进程 env,返回给前端的 raw 输出要把 token 替换成 `***`。
2. **yaml 回写必须用定点正则替换 + allow_unicode**,写前 yaml 解析校验目标值生效才落盘。**绝不能用 `yaml.safe_dump` 整体回写**——会把 settings.yaml/tracks.yaml 里的大量注释和多行 prompt 块全部抹掉。
3. **防编造红线**:/revise 和 ai_check 口径必须一致——缺来源只能删/软化,禁止新增来源/数字/日期。
4. **红档不给发布入口**(打穿自家质检闸)。
5. **CLI 必须用绝对路径调用**:CLI 装在 nvm 下(`/Users/<user>/.nvm/versions/node/vXX/bin/wechatsync`),uvicorn 进程 PATH 没有 nvm,只写 `wechatsync` 会找不到。子进程 env 还要把 node bin 目录加进 PATH。路径从 `WECHATSYNC_CLI_PATH` 环境变量读,有默认值兜底。
6. **不要复活 `publishers/` 那套 Playwright 发布器**(见下)。

---

## 七、历史包袱与"为什么是现在这样"(重要,别走回头路)

### 头条这堵墙(已充分验证的事实)
- **头条号没有官方发文 API**(公众号/百家号有)。2026-07 复查仍然如此。
- **头条没有"预存草稿"功能**——实测确认:手动都存不进草稿箱,草稿箱只收**发布失败**的稿。左下角"草稿保存中"是幌子。
- **头条敌视自动化**:Playwright 环境下会出现"保存失败"、卡顿;后台改版导致选择器失效(实测 `.add-icon` → `.syl-toolbar-tool.image`)。
- 社区共识:头条自动发布只有三条路——逆向内部 HTTP(易封号)、浏览器自动化(设置不好也可能封)、浏览器插件(最接近真人)。

### 所以选了 Wechatsync
- 开源 Chrome 扩展(5.8k star),**调各平台 Web 编辑器的官方接口**(不是模拟点击、不是爬虫),用浏览器已登录的 Cookie,数据不离开设备。
- **它反而能把内容送进头条草稿箱**(解开了"头条不能预存草稿"的死结)。
- 覆盖 29+ 平台,当前已登录:头条、百家号、知乎、微信公众号、X。

### 被放弃的方案(别再试)
- **`publishers/toutiao.py` (Playwright 直发)**:PUB-1 更新过选择器 + 加了人工闸 + dry-run(commit 2e356ba),但**从未真发过,已冻结**。理由:头条改版即失效、反自动化、且"一提交即公开"没有安全档。**留着当极端兜底,不要接进界面。**
- **`window.syncPost` JS SDK (A1, commit 8e5563b)**:article-syncjs 仓库老旧(2021, 38 star),新版扩展(v2.0.9)不再注入该接口,实测一直走降级提示。**已在 A2 中连根删除**(CDN引入、articleToHtml、ARTS 缓存全清)。
- **MCP 方式让 FlowX 后端发布**:与已通的 CLI 重复,更复杂,没采纳。

---

## 八、设计取舍(接手前先理解,别当 bug 改)

1. **「✓ 标记已发」为什么是手动的**
   FlowX **感知不到平台上的真实发布动作**——稿子进草稿箱后,用户在头条后台点发布,这个动作发生在平台侧。所以"已发"状态只能人工标记。曾讨论过"进草稿=已发",被否决:那会让"已发"tab 变成"已进草稿",用户会误以为都上线了,实际一篇都没公开。

2. **选题页赛道芯片(会话内) vs 设置页赛道开关(持久)是两层**
   设置页 = 长期开哪些赛道(写回 tracks.yaml);选题页 = 本次抓取范围(不落盘)。**后端先过 yaml 门槛**——设置里关掉的赛道,选题页芯片亮着也不会出现。这是有意的双层,不是 bug。

3. **热度不做全局排序**:各平台热度量纲不可比,硬排会把小平台全压到底。

4. **每赛道限 10 条在前端截断**:后端 /hotspots 契约不变,自动流水线仍能看到全量(它有自己的 count 控制)。

5. **`auto_publish` 开关是预留占位**:默认 false,打开也只打印"真发布功能待实现"。**结构预留是有意的**——等将来有干净的真发布手段再填实现,不用改架构。**不要用 Playwright 去填它**,除非明确接受封号风险。

6. **配图候选池弱排序的诚实局限**:中文新闻站 CDN 图 URL 多为哈希(无语义)、alt 缺失率高。有 alt 时明显有效,无 alt 时等于不变(但不会更差)。

---

## 九、已知痛点 / 未解决(接手重点)

### 🔴 P0:发布链路的稳定性(当前最痛,交接时正卡在这)
**现象**:`⚠ 自动发布失败:CLI 退出码 1` / `CLI 执行超时(120秒)`。
**根因**:Wechatsync 扩展的 **「MCP 连接」卡在"连接中…"**,CLI 连不上扩展。
**架构性原因**(必须理解):
```
FlowX后端 → CLI → [MCP连接:会话性、易断] → 浏览器扩展 → [登录态:会过期] → 平台
```
这条链有两个天生易断的环:MCP 连接(浏览器休眠/扩展挂起/Token 变动就断)、登录态(会过期)。
**所以"全自动无人值守"与这套架构有根本矛盾**:它要求一个活着的、连着的浏览器会话。

**建议方向**(按投入排序):
1. **加"发布前连接自检"**:发布前先探 CLI/扩展是否通,不通就**明确提示"请打开浏览器/重连扩展"**,而不是干等 120 秒超时。这是最低成本的体验改善。
2. **加自动重连/重试机制**。
3. 研究扩展 MCP 连接为什么卡"连接中"——可能与扩展版本、本地服务、Token 有关(交接时未查清)。

### 🟡 P1:封面无法自动设置
- 实测结论:`wechatsync sync --cover <url>` **CLI 接受该参数但头条不认**,草稿的「展示封面」仍为空,需人工在头条后台选。
- 试过两次(手动 CLI + 自动流水线),均未设上。
- **不是 FlowX 的 bug,是 Wechatsync/头条这条链的边界。**
- 可能出路:等 Wechatsync 更新、或试 MCP 的 `upload_image_file` + `sync_article` 组合(未验证)。

### 🟡 P1:classify 跨赛道泛词碰撞
- 现象:「曝光」在娱乐词表、「回应」在时事词表,而 classify 按**赛道定义顺序取首个命中**——"iPhone售价曝光"会被娱乐赛道吃掉。
- **建议方案**:改为**多词命中计分/关键词加权**,让匹配最强的赛道赢,而不是排最前的赢。
- 影响面:动 classify 核心,建议单独谨慎做 + 回归验证。

### 🟢 P2:其它
- 6小时去重上线**前**生成的旧稿没有时间戳记录,且兜底按标题匹配对不上被改写的标题——那些旧热点可能再出现一次(一次性,新数据都正确记录)。
- 优化后档位变化时,页4 顶部 tab 计数不实时刷新(点"↻刷新"或重进页4 即正确)。
- ai_check 对"已软化为观点表述但仍带数字区间"的内容仍偏严(compliance 可能给 60)。若绿档难拿,可在 prompt 补一句"已软化为观点表述的内容不再视为违规"。
- 编辑/手动修改/重新生成 等按钮仍是视觉占位,未接动作。
- 「教育」赛道未配置(选题页芯片现由 tracks.yaml 完全驱动,补个 yaml 条目即自动出现)。

---

## 十、后续可做的方向(原规划)

1. **主攻有官方 API 的平台**(⭐ 推荐):**百家号、微信公众号有官方发布 API**——那才是能做"真·稳定全自动"的地方(官方通道,不怕封、不怕改版、不用浏览器挂着)。头条接受半自动。这是"稳定全自动"的正道。
2. **多平台内容适配**:同一篇稿在不同平台最优形态不同(标题长度、排版),可做按平台适配。已登录:头条/百家号/知乎/公众号/X。
3. **上云 24h 无人值守**:需一台常驻机器 + 浏览器 + 扩展在线 + 登录态维护,与 Wechatsync 依赖对齐后才可行。
4. **通知升级**:目前只有日志可查,可加邮件/推送("今天自动发了几篇")。
5. **OpenClaw `axdlee/toutiao-publish`**(⚠️ 有风险):GitHub 上的头条全自动发布技能,号称能自动发布 + 自动从头条图库设封面 + 有重试。但底层仍是浏览器自动化,**承担封号风险**,且要引入 OpenClaw 环境。未实测。

---

## 十一、协作方式(原来怎么跑的,供参考)

- **Hans(产品/验收)**:定方向、真机实测(凡涉及真实账号/浏览器/扩展的操作只能他做)、拍板取舍。
- **Claude(技术顾问)**:规划、写可直接复制给 Code 的任务提示词、质检 Code 产出(重点看接线没断、安全红线、边界没越)。
- **Claude Code(施工)**:写代码、自测、反馈(改动清单/如何测/偏差)。

**纪律**:小步走、一模块一验证一 commit、复用引擎不重写、任务里写明"必须保住的接线清单"。

**Code 反馈里值得保留的习惯**:识别"规格本身有坑"时不机械执行(如用正则替换保留 yaml 注释而非 safe_dump 抹掉)、数据不支持理想方案时如实说明局限而非硬造(如百度无热度就留空、配图弱排序诚实交代边界)。

---

## 十二、关键 commit 索引(便于追溯)

| commit | 内容 |
|---|---|
| `6d51757` | 配图抓准(og优先+过滤+候选池) — 修了"挑文件最大的图"这个根因 |
| `db83f9c` | /reimage 一键换图 |
| `98ccc87` | UI R1:换 v4 桌面设计(左栏+沉红米黄),保留全部接线 |
| `0099799`/`2afc20f` | R2:设置页建全 + 来源开关驱动抓取 |
| `9352258` | A2:文章预览页 `/article/{id}`(供扩展提取),删除坏掉的 syncPost SDK |
| `78e06dd` | A3:后端调 CLI 自动发布 `/sync` |
| `4bb2c93` | 稿件状态流转(标记已发) |
| `ff77cac` | 热度值透传 |
| `36c3c83` | 配图池语义重排 |
| `713436e` | 6小时窗口去重 |
| `c618a35` | 补全 5 个赛道(财企/时事/健康/历史/美食) |
| `e413347` | 赛道拖拽排序 + 持久化 |
| `81f986d`/`b416f2b` | 定时无人值守流水线 |
| `d3d2ea7`/`333d313` | 自动流水线界面化(异步 + 免重启改时) |
| `2e356ba` | ⚠️ PUB-1 Playwright 发布器(已冻结,从未真发) |

---

## 十三、设计规范(UI)

设计基准:`product_mockup_v4.html`(在 repo 根目录)。

配色 token:
- 品牌砖红 `#AE352B`(只用于外壳:导航/按钮/强调)
- 米黄纸 `#F4ECDD`(页面底) / 暖白 `#FDFAF3`(卡片)
- 暖褐 `#2A231E`(正文) / 暖灰褐 `#7C7064`(次要)
- **质检三色刻意区别于品牌红**:绿 `#3B7A57` / 黄 `#B0801F` / **告警红 `#DE3A32`**(比品牌砖红更亮,只用于红档/拦截,配 ⛔ 图标)

**这条是硬约定**:品牌红和"红档红"必须一眼能分开,否则用户分不清"这是主题色"还是"这稿被拦了"。

无障碍:`:focus-visible` 焦点环、芯片/开关/热点条目均可 tab + Enter/空格激活、尊重 `prefers-reduced-motion`。
