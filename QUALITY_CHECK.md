# 质检环节 · 需求规格（QUALITY_CHECK）

> 在"生成+配图"和"进入待发"之间，加一道质量闸门。
> 新流程：抓热点 → 勾选 → 生成+配图 → **★质检★** → （绿/黄进待发 · 红拦下）→ 发布

---

## 一、质检方式：规则检测 + AI检测，合并出结果

### 1. 规则检测（代码，免费快，先跑）
逐项检查，命中即记为问题：

| 检查项 | 判定 | 严重度 |
|---|---|---|
| 标题断尾 | 标题以"的/了/是/这次/和/与"等虚词硬结尾，或含未闭合标点 | 严重 |
| 标题长度 | >30字 或 <8字 | 一般 |
| 正文字数 | <400字 或 >900字 | 一般 |
| "素材"字眼残留 | 正文含"素材/据资料/资料显示/上文提到"等 | 严重 |
| Markdown残留 | 正文含 `##` `**` 等符号 | 一般 |
| 大空格/空行 | 连续空行、异常空白 | 一般 |
| 无配图 | image 为空 | 一般 |

（很多规则项现有 writer.py 已有清理逻辑，可复用/参考。）

### 2. AI检测（DeepSeek 当审稿人）
把 标题+正文+配图搜图词 丢给 DeepSeek，用固定 prompt 让它输出 JSON：
```
{
  "logic_score": 0-100,        // 逻辑通顺度
  "fact_score": 0-100,         // 事实一致性（有无前后矛盾数字）
  "compliance_score": 0-100,   // 合规（有无绝对化/夸大/未证实当已证实）
  "image_match_score": 0-100,  // 配图与内容匹配度（据搜图词判断）
  "problems": ["问题1","问题2"] // 具体问题列表
}
```
AI检测的 prompt 要点：你是头条资深审稿编辑，从逻辑通顺、事实一致、合规、配图匹配四个维度评估，
指出具体问题，只输出 JSON。

### 3. 合并判定
- **总分** = 规则分（命中严重项-30、一般项-10，从100扣）与 AI 四项均分，取加权（建议规则占40% AI占60%）
- **问题列表** = 规则问题 + AI问题 合并

---

## 二、分数分档 + 处理

| 档 | 分数 | 处理 |
|---|---|---|
| 🟢 绿 | ≥80 | 通过，进待发 |
| 🟡 黄 | 60–79 | 进待发，但标黄提醒查看 |
| 🔴 红 | <60，或规则命中任一"严重"项 | **自动拦下，不进待发**，放"待修复"区 |

红档稿件：不进发布队列，用户可在界面点"重新生成"或"手动修改"后重新质检。

---

## 三、数据结构（存进稿件库）

articles 表增加字段：
```
qc_score    INTEGER     -- 质检总分 0-100
qc_level    TEXT        -- green / yellow / red
qc_problems TEXT        -- JSON 数组，问题列表
```
status 增加取值：`待修复`（红档稿件，区别于"未发"）

---

## 四、界面呈现（生成预览页 & 发布页）

每张稿件卡片加一块"质检结果"：
- 一个**分数徽章**（绿/黄/红 + 数字，如 🟢 88）
- **问题列表**（有则展开显示，如"配图与内容不符""标题偏短"）
- 红档卡片单独归入"待修复"标签页，带"重新生成/手动改"按钮

发布页的"未发"里只有绿/黄档；红档在"待修复"独立区。

---

## 五、实现建议（复用现有引擎）

- 新建 `quality/check.py`：
  - `rule_check(article_dict) -> (problems:list, deductions:int)`
  - `ai_check(title, body, image_query) -> dict`（调 generate.llm.chat）
  - `quality_check(article_dict) -> {score, level, problems}`（合并）
- 在流水线"配图后、save_article前"调 `quality_check`，把结果一起存库；
  red 档存 status="待修复"，其余 "未发"。
- FastAPI 加 `POST /recheck`（单篇重新质检）。

---

## 六、注意
1. AI检测每篇多一次 DeepSeek 调用（成本很低），但会慢一点，界面要有"质检中"状态。
2. 配图匹配度靠"搜图词 vs 文章"判断（我们有搜图词），不是真的看图内容——够用。
3. 阈值（80/60）先按此设，跑一阵后按实际效果调。
