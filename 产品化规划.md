# 头条内容工作流 · 产品化规划

> 把现有命令行引擎，做成有界面、可分发的桌面软件（形态参照 AIWriteX）。
> 定位：个人 + 少数人使用，每人填自己的 API，禁止商用，暂不商业化。

---

## 一、核心判断：你要的是"半自动"，不是"全自动"

从同类应用（壹伴、各类 AI 自媒体工作流）看，成熟产品都是
**选题 → 写作 → 配图 → 发布 → 复盘** 的闭环，且关键在"人机协作"：
AI 出初稿（约 80%），人负责筛选选题、审稿、打磨那 20%。

你梳理的 4 步流程，正是这个模式——机器干重活，你在两个关键节点把关：
**① 选题时勾选** 和 **③ 发布前确认**。这比纯全自动更安全、质量更可控。
产品的核心价值，就是把这套"人机协作流水线"做成点点鼠标就能用。

---

## 二、你的 4 步流程 → 界面设计

把你的想法细化成 4 个界面（Tab / 步骤页）：

### 页面1：选题设置
- **热点源选择**：免费源（百度、头条）默认开；付费源（博查等）可选，用哪个填哪个的 key
- **搜索源选择**：Tavily（免费）/ 博查（付费），可切换
- **赛道方向**：民生/科技/体育/娱乐 开关 + 名额权重（现在的 per_track_quota 图形化）
- 一个「抓取热点」按钮

### 页面2：选题筛选（勾选）← 你要的核心
- 抓到的热点按赛道分组列出：标题 + 来源 + 命中赛道
- 每条前面一个勾选框，你勾中意的
- 「开始生成」按钮 → 只处理勾选的
- （对应业内"筛掉 70%"的动作，质量从源头把关）

### 页面3：生成 + 预览
- 勾选的进入"搜索素材 → 改写 → 配图"，显示实时进度
- 生成完，每篇卡片展示：标题、正文、配图
- 可**就地编辑**、可**重新生成**、可**换图**
- 满意的 → 进入发布

### 页面4：发布管理（现在 dashboard 的升级版）
- **待发** / **已发** 两栏
- 待发的：可「立即发」「定时发」，可再编辑
- 已发的：显示状态、时间
- 状态自动流转（自动发的自动进已发）

> 另外保留一个「一键全自动」开关：熟练后可跳过勾选，选题→生成→定时发一条龙。
> 即"半自动"和"全自动"两种模式并存，你自己切。

---

## 三、技术架构（最大化复用现有引擎）

好消息：**最难的核心引擎你已经有了**，产品化主要是加界面层。

```
┌─────────────────────────────────────────┐
│  界面层（新增）：网页 4 个页面              │  ← 前端
│  HTML/CSS/JS（在现有 dashboard 上扩展）    │
├─────────────────────────────────────────┤
│  接口层（新增）：FastAPI                   │  ← 把下面的能力包成 HTTP 接口
│  /fetch_hotspots /generate /publish ...   │
├─────────────────────────────────────────┤
│  引擎层（已有，不动）：                     │  ← 现在的核心
│  hotspot / research / generate /          │
│  publishers / store                        │
├─────────────────────────────────────────┤
│  桌面壳（新增）：pywebview                  │  ← 把网页套成桌面窗口
├─────────────────────────────────────────┤
│  打包（新增）：py2app(Mac)/PyInstaller(Win)│  ← 变成可安装 App
└─────────────────────────────────────────┘
```

选型理由：pywebview + FastAPI 正是 AIWriteX 用的路线；你后端全是 Python，
FastAPI 能直接包现有逻辑；界面用网页，你的 dashboard 已经开了头。

---

## 四、分阶段落地（每阶段都能停、能用）

| 阶段 | 内容 | 产出 | 难度 |
|---|---|---|---|
| 0 | 命令行引擎 | ✅ 已完成 | — |
| 1 | FastAPI + 4 个网页页面 | 浏览器里点点就能用，比现在好用得多 | ★★★ 最大 |
| 2 | pywebview 套桌面壳 | 双击打开的桌面窗口，不用开浏览器 | ★★ |
| 3 | py2app/PyInstaller 打包 | .dmg / .exe，别人不装 Python 也能用 | ★★ |
| 4 | 产品化细节 | 配置向导、key 加密、错误友好化、更新 | ★★ |

**建议先集中做阶段 1**——界面做出来，"选题→勾选→生成→发布"的体验就成型了，
这一步价值最高。阶段 2/3（套壳打包）等阶段 1 稳定后再做。

---

## 五、要提前定的几个设计点

1. **付费源接入**：界面上列出可选源（免费/付费），用户选哪个就填哪个 key，
   免费源始终可用。付费的（博查搜索、AI 生成图等）作为增强项。
2. **key 管理**：界面里填、加密存本地（不再手动改 .env），首次启动引导填写。
3. **数据隐私**：所有数据（稿件、key、登录态）都在用户本机，不上传，
   这也是卖点之一（参照同类产品"本地运行、隐私可控"）。
4. **合规**：README/启动页写明"禁止商用、仅个人使用"，和 AIWriteX 一致。
5. **别人的环境**：发布器选择器可能因头条改版失效，需保留"手动录制选择器"
   的后路，或做成可配置。

---

## 六、下一步建议

从**阶段 1 的页面 1+2（选题 + 勾选）**开始做——这是你 4 步流程里最有价值、
最能体现"人机协作"的部分。先用 FastAPI 起一个本地服务，做出"设置源 → 抓热点 →
勾选"这条链路，跑在浏览器里。跑通后再往后接生成页、发布页。

现有引擎不用重写，只是给它包一层接口和界面。
