# 把这个文件复制成 .env，填上你的三个 key（等号后面直接写，不要加引号）
# 复制命令：cp .env.example .env  然后编辑 .env
# .env 不会被打包，以后覆盖更新不会动它，key 填一次永久有效。

DEEPSEEK_API_KEY=在这里填你的deepseek_key
TAVILY_API_KEY=在这里填你的tavily_key
PEXELS_API_KEY=在这里填你的pexels_key
